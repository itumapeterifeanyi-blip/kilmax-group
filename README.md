Gas Filling Shop — Monorepo scaffold

Quick start:
1. Copy `.env.example` to `.env` and set secrets.
2. Start services:
   docker-compose up -d
3. In two separate terminals:
   npm run dev:backend
   npm run dev:frontend

This scaffold contains:
- apps/backend: NestJS + Prisma placeholders
- apps/frontend: Next.js + Tailwind placeholders
"# kilmax-group" 
